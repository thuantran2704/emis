# Command metadata

This folder contains structured command metadata for reusable GitHub Copilot slash commands.

Use these JSON files as a lightweight schema for command behavior, tone, and workflow.
